/// A simple wrapper for location.
/// If a location package is used, replace with its model.
class GeoLocation {
  final double latitude;
  final double longitude;

  GeoLocation(this.latitude, this.longitude);
}
